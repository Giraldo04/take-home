# URL de la Web App publicada desde Google Apps Script.
# Se usa tanto en build time (obtener productos) como en cliente (enviar orden).
# Si se deja vacío, la app usa datos de ejemplo (src/lib/sample-products.ts).
PUBLIC_APPS_SCRIPT_URL=
